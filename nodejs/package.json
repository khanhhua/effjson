{
  "name": "effjson-nodejs",
  "version": "0.0.1",
  "description": "Efficient JSON inflation and deflation",
  "keywords": [
    "JSON"
  ],
  "author": "Khanh Hua",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/*.d.ts",
  "files": [
    "dist"
  ],
  "exports": {
    "import": {
      "types": "./dist/types.js",
      "default": "./dist/index.js"
    }
  },
  "license": "ISC",
  "scripts": {
    "test": "node --import tsx --test src/*.spec.ts src/*/*.spec.ts",
    "test:file": "node --import tsx --test",
    "build": "rm -R dist && tsc -p tsconfig.prod.json"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2"
  }
}
